// Once compile test.c,they'll call compile_file().
// they'll pass the test scene in the filename.
// the out_filename will be what you want to compile it to.
// flags determine how it should compile it.
int compile_file(const char* filename, const char* out_filename, int flags)
{
    return 0;
}